# Android Release Signing Configuration
# 
# 使用说明：
# 1. 复制此文件为 keystore.properties (不会被提交到Git)
# 2. 填入你的实际签名信息
# 3. 将keystore文件放在项目根目录（或指定其他路径）
#
# 生成keystore命令：
# keytool -genkey -v -keystore podium-release.jks -keyalg RSA -keysize 2048 -validity 10000 -alias podium

# Keystore文件路径（相对于项目根目录）
storeFile=podium-release.jks

# Keystore密码
storePassword=your_keystore_password_here

# Key别名
keyAlias=podium

# Key密码
keyPassword=your_key_password_here
